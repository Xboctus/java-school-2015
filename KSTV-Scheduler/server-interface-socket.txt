 - Будучи успешно инициализированным, сервер готов принимать соединения от клиентов на порт обратной связи.
 - После принятия сервером соединения от клиента между ними начинается взаимодействие, состоящее из передачи сообщений в заданном формате.

 - Сообщение — (синтаксически) последовательность частей сообщения.
 - Часть сообщения — (лексически) последовательность символов, не содержащая ограничителей сообщения и ограничителей части сообщения.
 - Формат отправки сообщения сервером: (часть сообщения + ограничитель части сообщения)* + ограничитель сообщения.
 - Формат приёма сообщения сервером: (часть сообщения + ограничитель части сообщения)* + остаток сообщения + ограничитель сообщения.
 - Остаток сообщения лексически имеет форму части сообщения и игнорируется сервером.
 - Ограничитель сообщения — символом U+001E.
 - Ограничитель части сообщения — символом U+001F.

 - Сообщения от клиента к серверу выполняют роль команд, на которые сервер даёт ответы.
 - Первая часть сообщения в команде определяет её тип, последующие являются её аргументами.
 - Если сервер получает команду, тип которй не известен ему, то он отправляет клиенту ответ "unknown_command".
 - Если сервер получает команду с количеством аргументов, не соответствующим типу команды, то он отправляет клиенту ответ "invalid_args_count".
 - Если в процессе обработки команды происходит внутренняя ошибка сервера, то но отправляет клиенту ответ "internal_error".
 
 - Тайм-аут неактивности клиента (отсутствия посылаемых им команд, т.е. ограничителя сообщения) — 30 мин.
 - Если сервер зафиксировал конец потока ввода от клиента до обнаружения ограничителя сообщения, то он производит оповещение этого клиента об ошибке ввода/вывода.

 - Определены следующие команды:
 1) Логин: login <name> <password>
 - Получив имя <name> и пароль <password> пользователя, сервер проверяет их корректность и аутентичность.
 - Ответ: login_invalid — в случае неуспеха, login_valid — в случае успеха.
 - Эффект: в случае успеха — текущее соединение закрепляется за переданным именем пользователя.
 2) Логаут: logout
 - Ответ: logout_ack
 - Эффект: текущее соединение не закреплено ни за каким именем.
 3) Отключение: disconnect
 - Ответ: disconnect_ack
 - Эффект: соединение закрыто сервером.
 4) Продление тайм-аута ожидания команд: prolongate
 - Ответ: prolongate_ack
 - Эффект: тайм-аут неактивности клиента сброшен.
 5) Инвалидация других соединений: invalidate
 - Ответ: invalidate_ack
 - Эффект: по всем соединениям, закреплённым за тем же именем, что и текущее соединение, кроме него самого послано оповещение инвалидации.

 - Сервер может отправлять клиенту сообщение по собственной инициативе.
 - Такие сообщение можно разделить на две группы: оповещения и сообщения о событии.

 - Определены следующие оповещения:
 1) Тайм-аут: timeout
 - Условие: тайм-аут неактивности клиента.
 - Эффект: сервер закрывает соединение.
 - Гарантируется, до истечения тайм-аута с времени отправки последней команды клиент не получит это оповещение.
 2) Инвалидация: invalidated
  - Условие: (любое из двух)
	* Получение по какому-либо другому соединению, закреплённому за тем же именем, что и текущее, команды "invalidate".
	* Изменение внешним образом пароля пользователя, за чьим именем закреплено текущее соединение.
 - Эффект: текущее соединение не закреплено ни за каким именем.
 3) Ошибка ввода/вывода: io_error
 - Условие: ошибка при использовании сервером текущего соединения.
 - Эффект: сервер закрывает соединение.
 - Данное оповещение может быть отправлено клиенту сразу после принятия соединения от него, если произошла ошибка инициализации этого соединения.
 4) Остановка сервера: shutdown
 - Условие: завершения сервером своей работы.
 - Эффект: сервер закрывает соединение.
 5) Подтверждение соединения: connect_ack
 - Условие: соединение успешно инициализировано.

 - Сообщение о событии отправляется при наступлении какого-либо события для пользователя, за именем которого закреплено текущее соединение.
 - Сообщение о событии состоит из следующий частей сообщения:
	* "event"
	* Локальное время, на которое было зарегистрировано данное событие.
	- Формат представления — количество миллисекунд, прошедших с начала эпохи до данного времени как локального в зоне GMT.
	* Текст события.
